# Server-only PostgreSQL connection. Never expose this as VITE_*.
# The @ in the password must be URL-encoded as %40.
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD%40HOST:PORT/postgres
JWT_SECRET=replace-with-a-long-random-secret
PORT=10000
FRONTEND_URL=https://fontmotaed.vercel.app
NODE_ENV=production
